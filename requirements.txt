# Install torch separately first, with the CUDA index for your GPU:
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
# then:
#   pip install -r requirements.txt
transformers==5.14.1
accelerate==1.14.0
